[project]
name = "site-panel-ssg"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "site-panel-shared",
  "site-panel-security",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/site_panel_ssg"]
