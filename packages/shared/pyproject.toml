[project]
name = "site-panel-shared"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.10.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/site_panel_shared"]
